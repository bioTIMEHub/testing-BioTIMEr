# Loading Alban's specific .Rprofile
if (
  isTRUE(Sys.info()[["user"]] == "sagouis") ||
    isTRUE(Sys.info()[["user"]] == "as80fywe")
) {
  source("~/.Rprofile")
}

base::options(renv.config.pak.enabled = TRUE)

source("renv/activate.R")

